<h1>Articles Main Viewer</h1>
<ul>
  <% @articles.each do |article| %>
    <li>
      <%= article.id %>:
      <%= link_to article.title, article %>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
